# Bitácora 06 — AMENDMENT: the censoring downgrade of P2's "shape" reading

**Date:** 2026-08-24
**Branch:** `main`
**Supersedes:** nothing outright; **qualifies** the mechanism reading of
bitacora 04 §4 and every downstream document that repeats it
(`HANDOFF.md`, `README.md` phase board P2 row, `docs/04-DECISIONS.md` P2 entry,
notebook 04 §6).
**Trigger source:** bitacora 05 (WP-A censoring probe); threshold pre-registered
in `PLAN-02-ext-research.md` §8 D5 before the probe ran.

---

## 1. The measured fact

Through this project's exact temporal feature pipeline and classifier protocol,
observation-window truncation **alone** — same generator, same rate, only the
window shortened — separates the two classes at AUC 0.9224–1.0000 across all
nine generator × window cells (bitacora 05 §2). The pre-registered amendment
trigger was AUC ≥ 0.85 on any cell; it fired at 1.0000.

## 2. What is downgraded

P2 supported H1's clauses as gated, but bitacora 04 §4 already found the
α-curves near-parallel and the mechanism undemonstrated. This entry strengthens
that caution into a formal bound:

> Any interpretation of SPEC_T's edge over COUNT as *distributional shape of
> behaviour* is bounded by a censoring artefact family whose ceiling through
> this pipeline is ≈ 1.0. Until WP-F's equal-window arms report, the honest
> statement of P2 is: "the spectrum adds +0.0367 over count; a same-generator
> different-window null shows this pipeline can extract ~all of its apparent
> signal from window differences alone; whether the corpus effect is behaviour,
> censoring, or both is open."

The clause verdicts themselves are NOT rewritten: H1 passed as pre-registered,
under the floors it named. What changes is the *reading*, everywhere.

## 3. Binding consequences

1. WP-F runs next-in-order after D/E and must include the equal-window arms;
   its figure draws the probe ceiling as a band.
2. Every SPEC-family number in FINDINGS/HANDOFF/notebooks from now on carries
   "(probe ceiling 1.00, bitacora 05)" beside it until superseded by WP-F.
3. WP-N/H4 framing cites this entry in its era-shift caveat.
4. Notebook 04 must present the probe result beside its current §6 narrative
   when rebuilt (builder change lands with WP-C's audit task).

## 4. What would lift the downgrade

An equal-window result (WP-F) in which clause-level deltas survive at K = 30 d
with the dim-matched controls, or a SHAPE-arm result demonstrably outside the
censoring ceiling's explanatory power. Neither exists yet. Per S4.3 the current
state is recorded as the finding.
